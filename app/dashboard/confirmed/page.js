import ConfirmedView from "@/components/dashboard/ConfirmedView";

export const metadata = {
  title: "Booking confirmed — Charter-IQ",
};

export default function ConfirmedPage() {
  return <ConfirmedView />;
}
